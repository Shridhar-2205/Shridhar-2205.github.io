# Local preview only. GitHub Pages builds this site server-side.
#   bundle install && bundle exec jekyll serve

source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "webrick", "~> 1.8"
